
Ghostwriter Agent
GW
Hello! How can I assist you today?
15:09
document.addEventListener('DOMContentLoaded', function() { const contentType = document.getElementById('contentType'); const documentUpload = document.getElementById('documentUpload'); const textInput = document.getElementById('textInput'); const socialMediaInput = document.getElementById('socialMediaInput'); const uploadForm = document.getElementById('uploadForm'); const fileInput = document.getElementById('fileInput'); // Toggle input sections based on content type contentType.addEventListener('change', function() { documentUpload.style.display = this.value === 'document' ? 'block' : 'none'; textInput.style.display = this.value === 'text' ? 'block' : 'none'; socialMediaInput.style.display = this.value === 'social' ? 'block' : 'none'; }); // Handle file upload area clicks documentUpload.addEventListener('click', function() { fileInput.click(); }); // Handle file selection fileInput.addEventListener('change', function() { // Handle file upload UI updates if (this.files.length > 0) { const fileList = Array.from(this.files) .map(file => file.name) .join(', '); document.querySelector('.upload-prompt p').textContent = fileList; } }); // Handle form submission uploadForm.addEventListener('submit', async function(e) { e.preventDefault(); const formData = new FormData(); switch (contentType.value) { case 'document': Array.from(fileInput.files).forEach(file => { formData.append('files', file); }); break; case 'text': formData.append('text', document.getElementById('sourceText').value); break; case 'social': formData.append('profile_url', document.getElementById('profileUrl').value); formData.append('post_count', document.getElementById('postCount').value); formData.append('sort_order', document.getElementById('sortOrder').value); break; } try { const response = await fetch('/api/source-content', { method: 'POST', body: formData }); if (response.ok) { showNotification('Content uploaded successfully'); loadContentLibrary(); // Refresh content list } else { throw new Error('Upload failed'); } } catch (error) { showNotification('Failed to upload content', true); console.error('Upload error:', error); } }); // Initialize content library loadContentLibrary(); }); // Function to load content library async function loadContentLibrary(page = 1) { try { const sourceFilter = document.getElementById('sourceFilter').value; const response = await fetch(`/api/source-content?page=${page}&source=${sourceFilter}`); const data = await response.json(); // Update content list const contentList = document.getElementById('contentList'); contentList.innerHTML = data.items.map(item => `
${item.title}
${item.source} • ${item.date}
`).join(''); // Update pagination updatePagination(data.total, data.page, data.pages); // Reinitialize Lucide icons for new content if (window.lucide) { lucide.createIcons(); } } catch (error) { console.error('Error loading content:', error); showNotification('Failed to load content library', true); } } // Function to update pagination function updatePagination(total, currentPage, totalPages) { const pagination = document.getElementById('pagination'); pagination.innerHTML = ` Page ${currentPage} of ${totalPages} `; // Reinitialize Lucide icons for pagination if (window.lucide) { lucide.createIcons(); } }